# HOW TO VALIDADTE IF SOMETHING IS INSIDE A STRING
# FOR THIS EXAMPLE IT IS A PASSWORD CHECKING PROGRAM

import string

specialC = "!@#$%^&*()-_=+[]{}|;:',.<>/?"


while True:
    password = input("Enter your password: ")
    
    has_upper = any(char.isupper() for char in password)             # <------ checks for upper cases
    has_number = any(char.isdigit() for char in password)            # <------ checks for digits
    has_special = any(char in specialC for char in password)          #  <------ checks for special characters
    
    if has_upper and has_number and has_special:
        print("Password is valid!")  
        break
    else:
        print("Password must include at least one uppercase letter, one number, and one special character. Try again.")
